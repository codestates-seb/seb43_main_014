package com.cv.domain.career.repository;

import com.cv.domain.career.entity.CareerSkillStack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CareerSkillStackRepository extends JpaRepository<CareerSkillStack, Long> {
}
