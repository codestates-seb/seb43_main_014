package com.cv.domain.cv.repository;

import com.cv.domain.cv.entity.CvSkillStack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CvSkillStackRepository extends JpaRepository<CvSkillStack, Long> {
}
