package com.cv.domain.project.repository;

import com.cv.domain.project.entity.ProjectSkillStack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectSkillStackRepository extends JpaRepository<ProjectSkillStack, Long> {
}
